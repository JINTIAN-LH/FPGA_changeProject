# FPGA 侧文档入口

本目录仅保留 FPGA 侧文档入口说明，正式设计与接口文档统一维护在仓库 `doc/` 目录。

## 1. 建议阅读顺序

1. `README.md`
2. `doc/系统总体架构设计.md`
3. `doc/通信协议接口控制文档 (ICD).md`
4. `doc/数据字典.md`
5. `doc/FPGA 模块详细设计.md`

## 2. 使用原则

1. 以仓库内 `.v/.sv` 代码为最终落地基线。
2. 文档中的接口定义以 ICD 为准。
3. 任何接口字段调整都必须同步更新 TB 与文档。
